/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable the app directory
  experimental: {
    // appDir is stable in Next.js 13.4+, no longer experimental
  },
}

module.exports = nextConfig